import 'package:flutter/material.dart';

class SprintDurationPage extends StatelessWidget {
  const SprintDurationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Sprint Süresi Sayfası'),
      ),
    );
  }
} 